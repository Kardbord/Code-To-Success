// Activate Quokka for current file by hitting ctrl+k
// Quit Quokka with ctrl+q
// Note that these are my custom keybindings. 

function sum(num1, num2) {
    return num1 + num2;
}

console.log(sum(2, 5));